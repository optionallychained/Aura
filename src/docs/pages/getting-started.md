# Getting Started [WIP]

```typescript
// highlighting

import { Game, Vec2 } from '@aura/2d';

const game = new Game({
    canvasDimensions: new Vec2(800, 600),
    init: () => {
        console.log('init')
    }
});
```
